# PostgreSQL 18 Docker Cluster

## Overview

This project provides a local PostgreSQL 18 cluster using Docker with:

- 1 Primary node
- 3 Read-only replicas (streaming replication)
- Asynchronous replication
- Host-accessible ports for each node
